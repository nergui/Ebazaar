package presentation.data;


public class LoginData {
	/*
	public void authenticate(LoginWindow loginWindow) throws UserException, BackendException {
		Integer id = Integer.parseInt(loginWindow.getCustId());
    	String pwd = loginWindow.getPassword();
    	Login login = new Login(id,pwd);
    	try {
    		usecaseControl.authenticate(login);
    		loadCustomer(login);
    	} catch(UserException e) {
    		throw(e);
    	} catch(BackendException e) {
    		throw(e);
    	}
    	
	}
    public void loadCustomer(Login login) throws BackendException {
        CustomerSubsystem cust = usecaseControl.prepareCustomerObject(login);
        SessionContext context = SessionContext.getInstance();
        context.add(CustomerConstants.LOGGED_IN, Boolean.TRUE);
        context.add(CustomerConstants.CUSTOMER, cust);

        
    }*/
	
}
